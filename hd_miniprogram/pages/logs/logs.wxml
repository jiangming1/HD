<!--logs.wxml-->
<view class="container log-list">
  <block wx:for="{{logs}}" wx:for-item="log">
    <text class="log-item" bindtap='scan'>{{index + 1}}. {{log}}</text>
  </block>
  <text wx:if="{{text}}">
    {{text}}
  </text>
  <button wx:if="{{text}}">保存</button>
</view>
